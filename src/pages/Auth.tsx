import { useState } from "react";

const Auth = () => {
  return (
    <div className="min-h-screen flex items-center justify-center">
      <h1>Auth Page Debug</h1>
    </div>
  );
};

export default Auth;